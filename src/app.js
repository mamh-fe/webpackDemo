import { hot } from 'react-hot-loader/root';  // 16.6版本以上可能就失效了
import React from 'react';

function App() {
  return (
    <div className="App">webpack go on...</div>
  );
}

export default hot(App);